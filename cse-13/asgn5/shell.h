#ifndef __SHELL_H__
#define __SHELL_H__

#include <stdbool.h>

//shell_sort takes any array and sorts is using shell sort algorithm
//array: any size array that needs to be sorted
//length: length of the given array

void shell_sort(int *array, int length);

#endif
